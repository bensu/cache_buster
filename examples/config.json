{
  "cache_buster": {
    "target_path": "target",
    "patterns": "examples/*",
    "dictionary": "target/cached_paths.json"
  },
  "other_unrelated": {
    "fields": "that we don't care about"
  }
}
