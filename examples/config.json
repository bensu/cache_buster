{
  "cache_buster": {
    "target_path": "target",
    "patterns": ["examples/*.js", "examples/*.css", "examples/*/*.txt"],
    "asset_path": "examples",
    "marker": "something",
    "manifest": "target/cached_paths.json"
  },
  "other_unrelated": {
    "fields": "that we don't care about"
  }
}
